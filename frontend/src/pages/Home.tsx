import React, { useState } from 'react';
import axios from 'axios';

const Home: React.FC = () => {

    return (
        <div>
            Tela de home
        </div>
    );
};

export default Home;
